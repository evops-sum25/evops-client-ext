[package]
name = "evops-uniffi"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
evops-markdown = { workspace = true, features = ["uniffi"] }

uniffi = { workspace = true, features = ["cli"] }

[lints]
workspace = true
